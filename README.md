# Все, что я уже знаю, значит?

#### На самом деле, не так уж много. 

1. **MD** похож на _HTML_, поээтому в нем разбираться не пришлось ~~карасики~~

2. Стартем с
```bash
git init
git add
git remote add origin <name>
git commit -m ""
git push -u origin main
```

3. Далее короче там всякие штуки команды все такое в общем все получается так красиво да вилки ложки поварешки. Коммиты крутятя, пулл-реквесты мутятся, всем удачи всем пока